apiVersion: v1
kind: Service
metadata:
  name: {{ include "smartthings-metrics.fullname" . }}
  labels:
    {{- include "smartthings-metrics.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: 9000
      targetPort: http-metrics
      protocol: TCP
      name: http-metrics
  selector:
    {{- include "smartthings-metrics.selectorLabels" . | nindent 4 }}
